<head>
    <%- include('head') %>
    <% if(page.cdn){ %> 
        <%- js(page.cdn.css) %>
    <% } %> 
    <%- page.preview.style ? tag('style', page.preview.style) : '' %>
</head>

<%- tag('body',{style:'margin: 0;'},[
    page.preview.html ? page.preview.html : '',
    page.cdn ? js(page.cdn.js) : '',
    page.preview.script  ? tag('script', page.preview.script) : ''
]) %>
